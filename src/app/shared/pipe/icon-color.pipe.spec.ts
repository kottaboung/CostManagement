import { IconColorPipe } from './icon-color.pipe';

describe('IconColorPipe', () => {
  it('create an instance', () => {
    const pipe = new IconColorPipe();
    expect(pipe).toBeTruthy();
  });
});
